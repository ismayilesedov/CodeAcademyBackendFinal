@model List<Subscribe>
@{
    ViewData["Title"] = "Index";
}

@section Css{
    <link href="~/Admin/build/css/custom.min.css" rel="stylesheet">
}
<section class="pcoded-main-container">
    <div class="pcoded-content">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-body table-border-style">
                        <div class="table-responsive">
                            @if (Model.Count() > 0)
                            {
                                <table class="table table-hover">
                                    <thead>
                                        <tr>
                                            <th style="font-size: 20px;">Email</th>
                                            <th style="font-size: 20px;">Options</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var contact in Model)
                                        {
                                            <tr data-id="@contact.Id">
                                                <td>@contact.Email</td>
                                                <td>
                                                    @if (User.IsInRole("SuperAdmin"))
                                                    {
                                                        <a class="delete-subscribe" data-toggle="tooltip" data-original-title="Remove">
                                                            <i class="fa-solid fa-trash" style="color: #da3e45;"></i>
                                                        </a>
                                                    }

                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>


@section Scripts{
    <script src="~/Admin/src/js/index.js"></script>

}



