@model List<WebSiteSocial>

@{
    ViewData["Title"] = "Index";
}
@section Css{
    <link href="~/Admin/build/css/custom.min.css" rel="stylesheet">
}
<section class="pcoded-main-container">
    <div class="pcoded-content">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-body table-border-style">
                        <div class="table-responsive">
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th>Icon</th>
                                        <th>Link</th>
                                        <th>Option</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var social in Model)
                                    {
                                        <tr>

                                            @if (social.Icon.Contains(".png") || social.Icon.Contains(".jpg") || social.Icon.Contains(".jpeg"))
                                            {
                                                <td>
                                                    <div class="image"><img class="img-fluid" src="~/assets/images/@social.Icon" /></div>
                                                </td>
                                            }
                                            else
                                            {
                                                <td>
                                                    @social.Icon
                                                </td>
                                            }
                                            <td>
                                                @social.Link
                                            </td>
                                            <td>
                                                <a asp-action="Edit" asp-route-id="@social.Id" data-toggle="tooltip" data-original-title="Edit">
                                                    <i class="fa-solid fa-pen-to-square" style="color: #16cac7;"></i>
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>

                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts{
    <script src="~/Admin/src/js/index.js"></script>
}


